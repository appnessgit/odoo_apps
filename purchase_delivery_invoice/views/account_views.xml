<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data>
        <record id="view_move_form_inherit" model="ir.ui.view">
            <field name="name">account.move.form.inherit</field>
            <field name="model">account.move</field>
            <field name="inherit_id" ref="account.view_move_form"/>
            <field name="priority" eval="8"/>
            <field name="arch" type="xml">
                <xpath expr="//field[@name='ref']" position="after">
                    <label for="purchase_picking_ids" string="Receipts" attrs="{'invisible': [('type', 'not in', ['in_invoice', 'in_refund'])]}"/>
                    <div class="o_row" name="purchase_picking_ids" attrs="{'invisible': [('type', 'not in', ['in_invoice', 'in_refund'])]}">
                        <field name="purchase_picking_ids" widget="many2many_tags"
                           attrs="{'readonly': [('state', '!=', 'draft')], 'invisible': [('type', 'not in', ['in_invoice', 'in_refund'])]}"
                           domain="[
                             ('picking_type_code', '=', 'incoming'),
                             ('partner_id', '=', partner_id),
                             ('fully_billed', '=', False),
                             ('purchase_id', '!=', False),
                             ('state', '=', 'done')]"
                           options="{'no_create': 1, 'no_open': 1}"
                        />
                        <span class="oe_edit_only" attrs="{'invisible': [('state', '!=', 'draft')]}">
                            <button name="get_purchase_lines_from_pickings" context="{'check_move_validity': False}" icon="fa-refresh" type="object" help="Add delivery lines"/></span>
                    </div>
                </xpath>
                <xpath expr="//page[@id='invoice_tab']/field[@name='invoice_line_ids']/form/sheet/field[@name='name']" position="after">
                    <notebook attrs="{'invisible': [('type', 'not in', ['in_invoice', 'in_refund'])]}">
                        <page string="Received Items" attrs="{'invisible': [('has_stock_moves', '=', False)]}">
                            <field name="type" invisible="1"/>
                            <field name="stock_move_ids" readonly="1">
                                <form>
                                    <group>
                                        <field name="product_id"/>
                                        <field name="move_line_id" invisible="1"/>
                                        <field name="stock_move_id"/>
                                        <field name="quantity"/>
                                        <field name="state" invisible="1"/>
                                    </group>
                                </form>
                                <tree>
                                    <field name="product_id"/>
                                    <field name="move_line_id" invisible="1"/>
                                    <field name="stock_move_id"/>
                                    <field name="quantity"/>
                                    <field name="state" invisible="1"/>
                                </tree>
                            </field>
                        </page>
                    </notebook>
                    <field name="has_stock_moves" invisible="1"/>
                </xpath>
            </field>
        </record>
    </data>
</odoo>